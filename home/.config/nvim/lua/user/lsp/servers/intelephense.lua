require("user.lsp.utils").config("intelephense", {
    filetypes = { "php", "blade" },
    settings = {
        intelephense = {
            format = {
                enable = false,
            },
        },
    },
})
