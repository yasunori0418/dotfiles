# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
#
# This config file uses keycodes (bindsym) and was written for the QWERTY
# layout.
#
# To get a config file with the same key positions, but for your current
# layout, use the i3-config-wizard
#

# Keybinds {{{
# Use these keys for focus, movement, and resize directions when reaching for
# the arrows is not convenient
set $left h
set $down j
set $up k
set $right l

# Mod key(windows layout)
set $mod Mod4

# Alt key(windows layout)
set $alt Mod1
# }}}

include $HOME/.config/i3/config.d/startup.conf
include $HOME/.config/i3/config.d/bindkey.conf
include $HOME/.config/i3/config.d/window.conf
include $HOME/.config/i3/config.d/workspace.conf

# vim:fdm=marker:fen:ft=i3config
