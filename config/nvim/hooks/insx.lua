-- lua_add {{{

-- }}}

-- lua_source {{{

require("insx.preset.standard").setup({
    cmdline = {
        enabled = true,
    },
    fast_break = {
        enabled = true,
    },
    fast_wrap = {
        enabled = true,
    },
    spacing = {
        enabled = true,
    },
})

-- }}}
